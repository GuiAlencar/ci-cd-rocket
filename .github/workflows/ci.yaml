name: CI

on: 
  push:
    branches:
    - main

jobs:
  build:
    name: 'Build and Push'
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Setup node
      uses: actions/setup-node@v4
      whith:
        node-version: 20
        cache: 'yarn'
    - run: yarn
    - run: yarn run test